import { type AgIconName, _ModuleSupport } from 'ag-charts-community';
export interface MenuItem<Value = any> {
    label: string;
    value: Value;
    icon?: AgIconName;
}
export declare class Popover extends _ModuleSupport.BaseModuleInstance implements _ModuleSupport.ModuleInstance {
    private readonly ctx;
    private readonly element;
    private anchor?;
    private removeGlobalEventListeners?;
    private readonly moduleId;
    constructor(ctx: _ModuleSupport.ModuleContext, id: string);
    show<Value = any>(opts: {
        items: MenuItem<Value>[];
        value?: Value;
        onPress?: (item: MenuItem<Value>) => void;
        onClose: () => void;
        class?: string;
    }): void;
    setAnchor(anchor: {
        x: number;
        y: number;
    }): void;
    hide(): void;
    isChildElement(element: HTMLElement): boolean;
    private updatePosition;
}
